version: 2

models:
  - name: int_price_history_by_vin
    description: Price trajectory per VIN — first price, min/max, drop count, observations.
    columns:
      - name: vin
        data_tests:
          - unique
      - name: first_price
        data_tests:
          - not_null
