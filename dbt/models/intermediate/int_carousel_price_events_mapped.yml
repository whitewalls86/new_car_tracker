version: 2

models:
  - name: int_carousel_price_events_mapped
    description: Tier-2 price-only events from carousel hints that are currently mappable to VIN.
    columns:
      - name: vin
        data_tests:
          - valid_vin_nullable
      - name: listing_id
      - name: artifact_id
      - name: observed_at
      - name: price
