version: 2

models:
  - name: int_model_price_benchmarks
    description: National price benchmarks by make/model/trim from active listings (last 3 days).
    columns:
      - name: make
        data_tests:
          - not_null
      - name: model
        data_tests:
          - not_null
      - name: national_listing_count
        data_tests:
          - not_null
