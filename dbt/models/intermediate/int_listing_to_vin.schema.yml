version: 2

models:
  - name: int_listing_to_vin
    description: >
      Latest VIN observed for each listing_id, derived from SRP and detail observations.
      Detail observations allow carousel hints to be mapped to VINs after scraping.
    columns:
      - name: listing_id
        data_tests:
          - not_null
          - unique

      - name: vin
      - name: vin_observed_at
        data_tests:
          - not_null

      - name: vin_artifact_id
