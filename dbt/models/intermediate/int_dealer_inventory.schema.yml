version: 2

models:
  - name: int_dealer_inventory
    description: Active inventory count per dealer per make/model. Higher counts signal more negotiation leverage.
    columns:
      - name: seller_customer_id
        data_tests:
          - not_null
      - name: dealer_inventory_count
        data_tests:
          - not_null
