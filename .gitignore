.venv/
.env
__pycache__/
*.pyc
support_bundle_*.zip
support_bundle_*/
.idea/
dbt/target/
dbt/dbt_packages/
dbt/package-lock.yml
dbt/logs/
dbt/.*.yml
.claude/*
!.claude/skills/
# Hooks are repository policy, not a personal preference, so the shared
# settings file is tracked and settings.local.json stays ignored by the rule
# above. Plan 138 Stage 1c added the first one.
!.claude/settings.json
.coverage
coverage.xml
GPT-Review-*.md.txt
target/
ssh-key*
docker-compose.override.yml
scripts/provision_oracle_vm.py
.codex-notes/
/audit*
graphify-out/
/rewrite*
.cache/
.tmp/
docs/.obsidian/

# Plan 162 Stage R. Where CI_RUN_RECORDS puts a run record. CI writes it
# into the workspace and uploads it; the documented local recipe names an
# absolute path, but the mutation proving the recorder's env gate writes a
# relative one, and it left this directory behind in the tree the first
# time it ran.
/ci-run-records/
